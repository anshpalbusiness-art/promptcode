{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["CI", "PROMPTCODE_DISABLE_SHARE"],
  "globalPassThroughEnv": ["CI", "PROMPTCODE_DISABLE_SHARE"],
  "tasks": {
    "typecheck": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "promptcode#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@promptcode/app#test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
